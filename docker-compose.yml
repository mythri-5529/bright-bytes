version: '3.8'

services:
  web:
    build: .
    container_name: bright_bytes_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DEBUG=True
    volumes:
      - .:/app
    command: python app.py
